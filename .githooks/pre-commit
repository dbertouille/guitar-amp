#!/bin/sh
# Pre-commit hook: verify Rust formatting before allowing a commit.
# Mirrors the CI "Format check" step (cargo fmt --all --check).

if ! cargo fmt --all --check; then
	echo
	echo "error: formatting check failed. Run 'cargo fmt --all' and stage the changes." >&2
	exit 1
fi
